services:
  ai-finance:
    build:
      context: .
      dockerfile: web/Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    env_file:
      - path: web/.env
        required: false
    environment:
      NODE_ENV: production
      HOSTNAME: 0.0.0.0
      PORT: 3000
    restart: unless-stopped
